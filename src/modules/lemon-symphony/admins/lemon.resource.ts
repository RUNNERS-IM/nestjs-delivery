// Entity
import { LemonEntity } from '../entities/lemon.entity';

// Option
import { lemonResourceOptions } from './options/lemon.response.options';

// Main section
export const lemonResource = {
  resource: LemonEntity,
  options: lemonResourceOptions,
};
